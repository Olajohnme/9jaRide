name: 9jaRide
description: Nigerian ride-sharing app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  supabase_flutter: ^2.8.0
  paystack_flutter: ^1.0.1
  qr_flutter: ^4.2.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

flutter:
  uses-material-design: true
  assets:
    - assets/
